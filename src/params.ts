import { iInitValue } from "./types";

// initialize the scene
export const initValue: iInitValue = {
  water: 200000,
  sky: 400000,
  cameraPosition: { x: 10000, y: 16000, z: 25000 },
  controlsTarget: { x: 0, y: 3000, z: 0 },
};
